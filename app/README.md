# MultiPlay (frontend)

PWA em Vue 3 + Vite para o catálogo de consoles e jogos com status por usuário.

```bash
npm install
cp .env.example .env
npm run dev
```

`VITE_API_URL` aponta para a API Laravel (padrão `http://localhost:8000/api`).
